---
services:
  jellyfin:
    image: jellyfin/jellyfin
    container_name: jellyfin
    user: 0:0
    network_mode: host
    volumes:
      - /container-storage/jellyfin/config:/config
      - /container-storage/jellyfin/cache:/cache
      - /container-storage/jellyfin/media:/media:ro
    restart: unless-stopped
